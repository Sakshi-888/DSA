#include<iostream>
using namespace std;
int main()
{
   vector<int> arr;
   arr.push_back(0);
   arr.push_back(3);

   arr.push_back(8);

   arr.push_back(9);

for(int i=0;i<5;i++)
{
    cout<<arr[i];
}
return 0;
}